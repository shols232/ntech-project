import random
import string

def man():
	
	fname = input("Enter your first name: ")
	lname =input ("Enter your lastname: ")
	email = input ("Enter  your email address: ")




	def randomString(stringLength=5):
 
    	letters= string.ascii_lowercase
    	choice = input("YOU ENTERED A WRONG KEYWORD: TRY AGAIN").lower()
	
	
	if User_choice == 'yes':
		print(f'Your details are printed below')
	
	
	elif User_choice =='no':
		user_password= input ("please enter your desired password: ")
		while len(user_password) < 7:
	     	 	print("Please enter a password that is greater than 7 characters: ")
	     	 user_password=input("  TRY AGAIN: ")


	user_details ={" Your first name is"+
fname,
	"Your last name is" + lname,
	"Your password is" + user_password
}
	for items in user_details:
		
		print (items)
     
	restart =input ("Do you want to restart : Enter yes or No").lower()
	if restart =='yes':
	
	man()
	
else:
	exit()
